# Key Vault pattern tests

Expected validation includes descriptor schema conformance, request schema conformance, Bicep build success, and verification that secure defaults such as purge protection and disabled public network access are represented in the template. CI/CD and .NET tests execute the actual checks.
