# Internal Web App pattern tests

Pattern-level checks should validate descriptor schema conformance, input schema behavior, Bicep compilation, and agreement between documented outputs and template outputs. Automated execution is handled elsewhere; this folder captures intended assertions and provides a representative request body.
